jdk:
  - openjdk11

before_install:
  - echo "Using Java $JAVA_HOME"

install:
  - ./gradlew clean

script:
  - ./gradlew clean build --refresh-dependencies

before_cache:
  - echo "Build completed successfully"

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
